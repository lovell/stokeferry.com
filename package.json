{
  "name": "stokeferry.com",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "npx @11ty/eleventy --input=src --output=docs",
    "serve": "npx @11ty/eleventy --input=src --output=docs --serve",
    "import": "npx wordpress-export-to-markdown --output=src --year-folders=true --month-folders=true"
  },
  "devDependencies": {
    "@11ty/eleventy": "^1.0.2",
    "wordpress-export-to-markdown": "^2.2.2"
  }
}
